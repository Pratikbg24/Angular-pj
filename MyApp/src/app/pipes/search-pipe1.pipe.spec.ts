import { SearchPipe1Pipe } from './search-pipe1.pipe';

describe('SearchPipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
